@model List<string>
@{
    ViewBag.Title = "Store";
}
<h3>Browse Genres</h3>

<p>
    Select from @Model.Count() genres:</p>
<ul>
    @foreach (var product in Model)
    {
        <li>@Html.ActionLink(product, "Browse", new {type = product})</li>
    }
</ul>

